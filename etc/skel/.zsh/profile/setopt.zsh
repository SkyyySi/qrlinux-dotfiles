#!/usr/bin/env zsh
# Some build-in shell options. Some of these may already bet
# enabled due to grml's zshrc.

setopt appendhistory
setopt autocd
setopt correct
setopt histignorealldups
setopt nobeep
setopt nocheckjobs
setopt prompt_subst
setopt vi
